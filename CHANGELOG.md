# 更新日志

## v0.1 — 双模式基础功能

- **v0.1.0** 项目初始化：HTML 骨架 + 双模式切换 UI + GitHub 仓库
- **v0.1.1** 图片加载：File System Access API 优先 + input fallback，Canvas 高清预览
- **v0.1.2** 裁剪模式：拖拽新建裁剪框 + 拖拽移动裁剪框 + 比例锁定
- **v0.1.3** 修复：高清渲染 (devicePixelRatio) + 比例切换清除裁剪框 + 自定义比例 (含 9:16)
- **v0.1.4** 修复：自定义比例锁定 bug + 保存功能（原名-修改后.扩展名）
- **v0.1.5** 缩放模式：实时预览 + 预设尺寸按钮 + 保持比例联动

## v0.2 — 体验打磨

- **v0.2.0** 裁剪框八角拖拽手柄 + 九宫格辅助线 + 方向光标提示
- **v0.2.1** 撤销/重做：Ctrl+Z/Y 快捷键 + 50 步历史栈
- **v0.2.2** 滚轮缩放 + Space 拖拽平移 + Ctrl+0 重置缩放
- **v0.2.3** 放大后直接拖拽平移（无需按键）
- **v0.2.4** 修正：图片外拖拽平移，图片内拖拽裁剪，互不冲突
- **v0.2.5** 键盘快捷键（Enter/Esc/Delete）+ 深色/浅色主题切换
- **v0.2.6** 修复深色主题文字可见度

## 教训总结

1. Canvas 必须乘以 `devicePixelRatio` 才能在高分屏上清晰
2. 切换比例时应清除旧裁剪框，让用户重新拖拽，而非尝试调整
3. `getAspectRatio()` 函数的返回值必须与实际拖拽约束逻辑一致，否则自定义比例形同虚设
4. 图片外拖拽=平移、图片内拖拽=裁剪 的分区逻辑比条件判断（放大后平移）更符合直觉
5. File System Access API 的 `showSaveFilePicker` 不能直接写入原图文件夹，需用户通过另存为对话框选择位置
6. 深色主题需要全面提亮灰色变量并显式覆盖关键文字元素，仅靠变量映射不够
